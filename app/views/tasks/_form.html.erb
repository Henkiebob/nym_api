<div class="input_fields_wrap">
<%= simple_form_for @task do |f| %>

  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.input :deadline, as: :date, html5: true %>
  </div>
  <%= f.hidden_field :house_id, :value => session[:current_house_id] %>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>

</div>
